(dans script)
{
sudo apt install postgresql postgresql-contrib;
sudo -u postgres psql;
CREATE USER asahonet WITH PASSWORD 'oui';
CREATE DATABASE django_db OWNER asahonet;
GRANT ALL PRIVILEGES ON DATABASE django_db TO asahonet;
} => dans un docker pour db pour apr communication entre docker db et docker django


1- dl python3 + django
sudo apt update; sudo apt install python3 python3-pip Django==5.0
pip install pyscopg pyscopg2-binary

2- begin project
django-admin startproject pong


3- copie des fichiers dans le docker

4- config db
python3 manage.py migrate

